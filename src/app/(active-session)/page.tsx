import Shuffle from '@/components/Shuffle';

const Home = () => {
  return (
    <main className='flex h-full w-full flex-col items-stretch p-8'>
      <Shuffle />
    </main>
  );
};

export default Home;
